import { ReactNode, useState } from "react";

export default function CharacterList({ children }: { children: ReactNode }){
    



    return(
        <tbody>
            {children}
        </tbody>
    );
}